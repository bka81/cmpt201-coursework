#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>

struct header {
  uint64_t size;
  struct header *next;
  int id;
};

void initialize_block(struct header *block, uint64_t size, struct header *next,
                      int id) {
  block->size = size;
  block->next = next;
  block->id = id;
}

int find_first_fit(struct header *free_list_ptr, uint64_t size) {
  // TODO: Implement first fit
  struct header *current = free_list_ptr;
  while (current != NULL) {
    if (current->size >= size) {
      return current->id;
    } else {
      current = current->next;
    }
  }
  return -1;
}

int find_best_fit(struct header *free_list_ptr, uint64_t size) {
  int best_fit_id = -1;
  // TODO: Implement best fit
  struct header *current = free_list_ptr;
  uint64_t smallest = UINT64_MAX;
  while (current != NULL) {
    if (current->size >= size) {
      if (current->size < smallest) {
        best_fit_id = current->id;
        smallest = current->size;
        current = current->next;
      } else {
        current = current->next;
      }
    } else {
      current = current->next;
    }
  }
  return best_fit_id;
}

int find_worst_fit(struct header *free_list_ptr, uint64_t size) {
  int worst_fit_id = -1;

  // TODO: Implement worst fit
  struct header *current = free_list_ptr;
  int largest = 0;
  while (current != NULL) {
    if (current->size >= size) {
      if (current->size > largest) {
        worst_fit_id = current->id;
        largest = current->size;
        current = current->next;
      } else {
        current = current->next;
      }
    } else {
      current = current->next;
    }
  }
  return worst_fit_id;
}

void free_mem(struct header **free_list_ptr) {
  struct header *current = *free_list_ptr;
  while (current != NULL) {
    struct header *next = current->next;
    free(current);
    current = next;
  }
  *free_list_ptr = NULL;
}

int main(void) {

  struct header *free_block1 = (struct header *)malloc(sizeof(struct header));
  struct header *free_block2 = (struct header *)malloc(sizeof(struct header));
  struct header *free_block3 = (struct header *)malloc(sizeof(struct header));
  struct header *free_block4 = (struct header *)malloc(sizeof(struct header));
  struct header *free_block5 = (struct header *)malloc(sizeof(struct header));

  initialize_block(free_block1, 6, free_block2, 1);
  initialize_block(free_block2, 12, free_block3, 2);
  initialize_block(free_block3, 24, free_block4, 3);
  initialize_block(free_block4, 8, free_block5, 4);
  initialize_block(free_block5, 4, NULL, 5);

  struct header *free_list_ptr = free_block1;
  int first_fit_id = find_first_fit(free_list_ptr, 7);
  int best_fit_id = find_best_fit(free_list_ptr, 7);
  int worst_fit_id = find_worst_fit(free_list_ptr, 7);

  // TODO: Print out the IDs
  printf("The ID for the Best-Fit algorithm is: %d\n", first_fit_id);
  printf("The ID for the First-Fit algorithm is: %d\n", best_fit_id);
  printf("The ID for the Worst-Fit algorithm is: %d\n", worst_fit_id);
  free_mem(&free_list_ptr);
  return 0;
}
/* PART 2; COALESCING ALGORITHM
 * Note: I used chatgpt for some guidance in this part
 * struct node * next;
 * struct node * prev;
 * struct node * current = free_list_ptr 
 *
 * ## Walk through the free list until finding the location where new node belongs
 * ## by finding the node whose location is right after new node
 * while current != NULL AND current_addr < newnode_addr
 *    prev = current
 *    current = current-> next
 * 
 * #insert newnode
 * newnode->next= current
 * 
 * if prev == NULL : free_list_ptr = newnode
 * else : prev->next = newnode 
 * 
 * ## check if the block before newnode is adjacent and if so merge it
 * if prev!= NULL and (prev_addr_end) == (newnode_addr_start):
 *    ##merge
 *    prev->size = prev->size + newnode->size
 *    prev->next = newnode->next;
 *    newnode = prev; ## merged block
 *    
 * ## check if the block after newnode is adjacent and if so merge it.
 * next = newnode -> next;
 * if next!=NULL and newnode_addr_end == next_addrs_start
 *    newnode-> size = newnode->size + next->size
 *    newnode->next = next->next
 *
 * return head; 
 *
 */




